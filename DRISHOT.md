# מה אני צריכה — רשימת דרישות

מיקומים / כיול / geo. עדכון אחרון: 10 באוגוסט 2026.

---

## מנעה — צינור הרדאר

### 1. `radar_detections_all()` — הבקשה הקריטית

**מה היא עושה:** מחזירה את **כל** ההחזרים מכל פריים, כולל אלה שהסינון מסמן כסטטיים. לא רק המטרות הנעות.

**מה היא מחזירה — רשימה, ולכל החזר:**

- `range_m` — טווח במטרים
- `azimuth_deg` — אזימוט במעלות. צריך לדעת אם + הוא ימינה או שמאלה
- `elevation_deg` — זווית גובה במעלות
- `velocity_mps` — מהירות רדיאלית מדופלר, מטר לשנייה. **חיוני** — זה מה שמפריד סטטי מנע
- `snr_db` — עוצמת ההחזר בדציבלים
- `timestamp_ms` — חותמת זמן בשעון המשותף

**למה:** קירות הם החזרים סטטיים. הסינון שלכם בנוי למצוא אנשים נעים, ולכן מסיר בדיוק את מה שאני צריכה. אצלכם זה רעש, אצלי זה מקור המיקום היחיד.

בלי GPS רציף, אחרי הקיבוע הראשוני ה-IMU לבדו מחזיק 10 שניות עד שהמיקום סוטה ביותר ממטר. אחרי דקה — 35 מטר. הדרך היחידה לאפס את הסחיפה היא לזהות קירות ולהצליב מול המפה, וזו גם השיטה המקובלת בתעשייה לניווט ללא GPS.

---

### 2. `shared_clock_ms()` — יעד M0

**מה היא עושה:** מונה מונוטוני יחיד שכל החיישנים חותמים בו.

**מה היא מחזירה:**

- מספר שלם, מילישניות

**למה:** ב-18 קמ"ש הרכב עושה 5 מטר בשנייה. הפרש של 200 מילישניות בין הרדאר לתמונה הוא מטר של אי-התאמה — בטווח שכולו 20 מטר.

---

### 3. `radar_ego_velocity()` — נחמד שיהיה, לא חובה

**מה היא עושה:** מחזירה את מהירות ה-rig עצמו, שנגזרת מהמהירות המדומה של ההחזרים הסטטיים.

**מה היא מחזירה:**

- `speed_mps` — מהירות לאורך כיוון הנסיעה
- `yaw_rate_dps` — קצב סיבוב, מעלות לשנייה
- `sigma_speed_mps` — אי-ודאות במהירות
- `static_count` — כמה החזרים נכנסו לאמידה

**למה:** אין מד מהירות ברכב. בלי מהירות, המיקום דורש אינטגרל כפול מהמאיץ והשגיאה גדלה כריבוע הזמן. עם מהירות זה אינטגרל אחד בלבד.

**אם אתם לא מחשבים את זה — אחשב בעצמי מ-`radar_detections_all()`.** זו הסיבה שהיא לא חובה.

---

## מה-IMU

### 4. `imu_attitude()`

**מה היא עושה:** מחזירה את הזווית הנוכחית של ה-rig.

**מה היא מחזירה:**

- `roll_deg` — מכוח הכבידה. לא נסחף
- `pitch_deg` — מכוח הכבידה. לא נסחף
- `yaw_deg` — **יחסי בלבד.** נסחף 26 מעלות לדקה
- `yaw_sigma_deg` — אי-ודאות בכיוון, שגדלה מאז האיפוס האחרון
- `is_static` — האם ה-rig דומם ברגע הזה
- `timestamp_ms` — בשעון המשותף

**השדה הקריטי הוא `yaw_sigma_deg`.** בלעדיו אני לא יכולה לדעת אם הכיוון בן שנייה או בן דקה, וההפרש הוא בין 0.4 מעלות ל-26 מעלות.

אם אתם לא מחשבים אותו — תנו לי את **הזמן שעבר מאז איפוס ה-yaw** ואחשב בעצמי.

---

### 5. `imu_raw()`

**מה היא עושה:** מחזירה את הקריאות הגולמיות מהחיישן, בלי עיבוד.

**מה היא מחזירה:**

- `accel_mg` — תאוצה בשלושה צירים, במילי-g
- `gyro_dps` — קצב סיבוב בשלושה צירים, מעלות לשנייה
- `timestamp_ms` — בשעון המשותף

**למה גולמי ולא רק attitude:** הכיול בין ה-IMU למצלמה עובד על כיוון הכבידה, כלומר על המאיץ עצמו.

ובנוסף — המבחן "האם ה-rig דומם" לפי גודל התאוצה בלבד הוא חלש מאוד. בלימה של 300 מילי-g מטה את האנך ב-17 מעלות אבל מעלה את גודל התאוצה ב-44 מילי-g בלבד, כי זה מתחבר בריבועים. צריך את שלושת הצירים כדי לזהות את זה.

---

## מהמצלמה התרמית

### 6. `camera_geometry()`

**מה היא עושה:** מחזירה את שדה הראייה ומידות החיישן, כדי להמיר אזימוט לטור פיקסלים.

**מה היא מחזירה:**

- `hfov_deg` — שדה ראייה אופקי. אני מניחה 57
- `vfov_deg` — שדה ראייה אנכי. אני מניחה 44
- `width_px` — אני מניחה 160
- `height_px` — אני מניחה 120

**מה שצריך:** לאמת את ארבעת המספרים מול היחידה הפיזית. עדשה לא סטנדרטית משנה את השניים הראשונים ומזיזה כל סימון בפריים.

---

### 7. `thermal_frame()` — לתצוגה

**מה היא עושה:** מחזירה את הפריים התרמי החתום-זמן, כדי להניח עליו את השכבה.

**מה היא מחזירה:**

- `pixels` — מערך 160 על 120, רדיומטרי
- `timestamp_ms` — בשעון המשותף
- `ffc_in_progress` — האם כיול השדה רץ כרגע

**למה `ffc_in_progress`:** מלכודת 14 אצלכם מתעדת שה-FFC נראה כמו detection רועד. אם השכבה מצוירת על פריים כזה, מוטב שהיא תדע.

---

## מיקום ראשוני

### 8. `initial_fix()`

**מה היא עושה:** נותנת מיקום וכיוון בהפעלה. פעם אחת, לא זרם. מ-GNSS או מנעיצה ידנית של המשתמש.

**מה היא מחזירה:**

- `latitude` — מעלות, WGS84
- `longitude` — מעלות, WGS84
- `heading_deg` — מצפני, אפס הוא צפון. **אין לו שום מקור אחר במערכת**
- `sigma_position_m` — דיוק המיקום. GNSS רגיל 3 עד 8 מטר, בין בניינים 5 עד 20
- `sigma_heading_deg` — דיוק הכיוון
- `source` — אחד מ: `gnss`, `manual_pin`, `road_snap`
- `timestamp_ms` — מתי. זה מה שקובע את שחיקת האי-ודאות

**הערה:** `map_anchor.py` של משה כבר עושה את הנעיצה הידנית — מקבל `--latlon` ולחיצה אחת לכיוון. זה מסלול קיים, צריך רק לחבר אותו.

---

## הכרעה שצריכה להתקבל

### GNSS — כן או לא

החשש שהועלה היה ש-GPS רציף מסגיר את המיקום לאויב.

מקלט GNSS הוא **פסיבי**. הוא רק מקשיב לאותות מלווינים ולא פולט כלום, ולכן אי אפשר לאתר אותו דרך הקליטה שלו.

מה שכן מסגיר הוא **דיווח המיקום ברדיו**, וזה שידור אחר לגמרי.

ומה שכן בעיה אמיתית בשדה הוא **שיבוש והטעיה** — האויב מונע קליטה, או מזין מיקום מזויף.

אם קליטת GNSS מותרת, המפה הופכת מהפתרון היחיד לגיבוי, ומקבלת תפקיד נוסף: **לתפוס הטעיה.** אם ה-GPS טוען שאנחנו בתוך בניין או שקפצנו 200 מטר בשנייה, המפה יודעת שזה בלתי אפשרי.

**ההכרעה משנה את סדר העדיפויות מהיסוד**, ולכן היא ראשונה ברשימה למטה.

---

## סדר העדיפויות

אם אפשר לבקש דבר אחד בלבד — הראשון.

1. **`radar_detections_all()` עם ההחזרים הסטטיים.** בלעדיה המערכת מחזיקה 10 שניות. חוסמת את הכל.
2. **שעון משותף.** בלעדיו אי אפשר לשלב חיישנים בכלל.
3. **`imu_attitude()` עם `yaw_sigma_deg`**, או לפחות הזמן מאז האיפוס.
4. **הכרעה על GNSS.**
5. **אימות שדה הראייה** של הלפטון מול העדשה הפיזית.

---

## מה שכבר עובד אצלי — לא צריך לבקש

- גובה גאואידי: המרה בין מה ש-GNSS מדווח למה שהמפה אומרת. בירושלים ההפרש 19.83 מטר
- DEM: גובה קרקע ומשטח, ברזולוציה של 30 מטר
- מתארי מבנים: 96,823 בירושלים
- רשת דרכים ושבילים: 25,542
- כיול IMU מול מצלמה: הקוד מוכן, מדויק ל-0.17 מעלות בסימולציה. לא אומת בחומרה
- חיזוי מה שאמור להופיע בפריים: עובד, ואומת מול תמונה אמיתית מהשטח

---

## אזהרה שחייבת להישאר בכל פלט

**המפה סטטית והעולם לא.**

משאית חונה, מחסום, הריסות, בניין חדש — אף אחד מהם לא במפה. ה-DEM עצמו קפוא: הצילומים שממנו הוא נבנה הם מ-2011 עד 2015, אז כל בניין שנבנה מאז פשוט לא קיים בו.

לכן המערכת יכולה לומר **"כאן יש קיר"**, אבל **לעולם לא "כאן אין כלום"**.

בעשן ההבדל הזה הוא הבדל בטיחותי.
