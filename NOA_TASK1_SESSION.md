# משימה 1 — פרוטוקול סיום (נסגרה 10/08/2026)

הבדיקה רצה על ה-N6 החי (OpenMV v5.0.0) עם `board/mmwave/task1_timer_probe.py`.
מצלמה ורדאר היו מחוברים ב-USB; לא נדרש שום חיווט.

## ההחלטה — התצורה לקוד הסנכרון (משימה 4)

| מה | הערך שנבחר | למה |
|---|---|---|
| טיימר | **TIM2** | 32-ביט, ושני האותות עליו יחד = שעון אחד משותף, אפס סטייה |
| ערוץ ל-VSYNC תרמי | **CH3 דרך פין P4** | אומת עובד ב-IC |
| ערוץ ל-SYNC_OUT רדאר | **CH4 דרך פין P5** | אומת עובד ב-IC, במקביל ל-CH3 |
| שעון מקור | 400MHz | עם prescaler 199 → טיק של 500ns |
| התגלגלות המונה | כל **~35.8 דקות** (2^32 × 500ns) | חייבים unwrap ב-frame_clock — אבל נוח |

## תוצאות מדודות

### רוחב מונים (הוכרע אמפירית — כתיבת period וקריאה חזרה)
```
TIM2  width=32-bit  readback=0x7FFFFFFF  clock=400000000 Hz   ← הבחירה
TIM4  width=32-bit  readback=0x7FFFFFFF  clock=400000000 Hz   ← גיבוי (P7/P8)
TIM12 width=16-bit  → מתגלגל כל ~0.03s, לא רלוונטי
TIM17 width=16-bit  → לא רלוונטי
```

### שני ערוצי IC על TIM2 בו-זמנית
```
SUCCESS: TIM2 holds both IC channels at once
  P4 -> TimerChannel(timer=2, channel=3, mode=IC)
  P5 -> TimerChannel(timer=2, channel=4, mode=IC)
```

### בדיקת התנגשות עם המצלמה
```
camera initialized
SUCCESS: TIM2 dual-IC still works with the camera up
```
כלומר: צינור המצלמה של הפירמוור **לא** משתמש ב-TIM2 — אין קונפליקט.

## הערות

- חלק ב' של הסקריפט דיווח "כל ערוץ עובד על כל פין" — זו הצהרה אופטימית מדי של
  ה-API (הוא לא מאמת ניתוב ערוץ-לפין). המיפוי האמין הוא לפי ה-datasheet:
  P4/B10 = TIM2_CH3, P5/B11 = TIM2_CH4 — ואומת בפועל בחלק ג'.
- מלכודת שנשארת בתוקף: `time.ticks_us()` אסור כמקור זמן (מתגלגל כל 17.9 דק').
  מקור הזמן היחיד: המונה של TIM2 עצמו.
- תקלה שתוקנה בסקריפט: `period=0xFFFFFFFF` גורם overflow ב-MicroPython;
  משתמשים ב-`0x7FFFFFFF` לבדיקת הרוחב.

## מה זה פותח

משימה 4 (קוד הסנכרון) יכולה להיכתב כבר עכשיו סביב TIM2/CH3+CH4 — נשאר רק
החיווט הפיזי (משימות 2–3) כדי שיהיו פולסים אמיתיים לתפוס.
