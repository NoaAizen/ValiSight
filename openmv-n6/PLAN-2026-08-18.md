# תוכנית עבודה — 2026-08-18, מ-08:00

הבסיס: `PLAN-2026-08-17.md` (יום השטח שלא בוצע) + `WORK-PLAN.md` אחרי יום
השולחן של אתמול. **מסלול השולחן מוצה**: ‏Phase 0, D0, D1 סגורים; D2/D3 בנויים
וחסומים על נתוני שטח (P1) ועל אקסטרינזיקת שני-הצדדים (H5). כל שעה נוספת מול
המקלדת היא כוונון עיוור — היום הזה פיזי.

**סדר שאסור להפוך: טבלת פאזה ← נסיעה ← שערים ← הקלטות.** הקלטה עם boresight
לא מכויל מתייגת את המסלול הלא-נכון — זיהום שקט.

---

## 08:00–08:15 — לנעול את אתמול בגיט

`perception/` כולו untracked, ועוד רגע נשענים עליו בשטח:

```bash
git add perception/ WORK-PLAN.md PLAN-2026-08-18.md
git commit  # יום השולחן: Phase0+D0..D3, שני mp4 שתוקנו, COCO תרמי
```

## 08:15–09:15 — H1: טבלת הפאזה (שולחן, בית)

הצבה לפי `PLAN-2026-08-17.md` §2א: ‏TCR על חצובה, ‏1.5 מ' boresight, קודקוד
בגובה האנטנה, רחוק מרהיטים (שפת השולחן המתגלגל כבר תפסה כיול אחד), לא מול חלון.

```bash
cd tools/calib
./radar_measure_bias.py --frames 300 --out ../../calib-artifacts/bias-20260818.json
```

`DO NOT PASTE` → לתקן לפי מה שהכלי אומר ולחזור. עבר → להדביק ל-`radar_people.cfg`
במקום בלוק ה-UNVERIFIED, ואז האימות שלא מדלגים עליו (`measure=0`):

| בדיקה | שער |
|---|---|
| TCR בטווח | ‏1.50 ± 0.02 מ' |
| אזימוט boresight | ‏\|az\| ≤ 0.5° |
| `./tools/diag/radar_listen.py --stage1 --seconds 60` | ‏0 בייטים אבודים |

**שער עצירה: נכשל → לא נוסעים. היום הופך ליום איתור-תקלה בשולחן.**

## 09:15–10:00 — H2: לפטון 16-bit (מתוזמן-קשיח, 45 דק')

FFC לפני סשן + שמירת raw 16-bit ‏(38,400 B/פריים; ‏`thermal_len` כבר נתמך
ב-loader). תקציב לינק: תוספת ~19KB/פריים על ~45KB קיימים — לאמת עם stage-1.

**כלל עצירה: לא נסגר ב-45 דק' → מקליטים היום ב-8-bit וממשיכים.** לפי
`WORK-PLAN.md` H2 — כשל כאן דוחה את ערוץ A' בלבד; תוויות הרדאר לא צריכות 16-bit.

## 10:00–10:45 — לוגיסטיקה ונסיעה

לוודא **לפני** יציאה: 15 מ' פנויים, שני צידי אזימוט פתוחים, חשמל ל-Jetson.
לארוז: TCR + חצובה, מטר, ה-rig, כבלים, הקונפיג המכויל על הדיסק.

**אין אתר → לא מבזבזים את היום:** נשארים ומקליטים בבית את מה שלא צריך 15 מ' —
‏C1 (סצנה ריקה + מאוורר/דלת/וילון, 20 דק') + P2 (עמידה/ישיבה/כריעה ב-1.5–4 מ',
10 דק'), ובערב H1v בחושך. אתר נדחה למחר.

## 10:45–11:30 — H4: שער 1, מבחן היציאה של Mode P

הולך ב-**12 מ'**: זיהוי ≥90% מהפריימים, ‏v ≠ 0 על ההולך, אפס סימני aliasing
(זה סוגר גם את `max unambiguous vel ±4.99` שמסומן needs-confirmation בקונפיג).

**נכשל → לא מקליטים P1. מאבחנים** — Mode P לא עושה מה שנגזר, וזה ממצא בעצמו.

## 11:30–12:30 — H5: שער 2, אקסטרינזיקה ב-10 ו-15 מ'

תחנות TCR ב-10 ו-15 מ', **שני צידי האזימוט** (לפי `CAPTURE-PROTOCOL.md`),
‏`--view visible` בלבד. שער: ‏du בתוך ±50px.

זה הפריט שאתמול קיבל דחיפות חדשה: ‏D3 מדד את השארית התלויה-אזימוט בפועל —
‏±7px במרכז, ‏+15…+57px באגפים. התחנות האלה הן התיקון.

**נכשל → ממשיכים להקליט.** קונפיג ופאזה נצרבים להקלטה; אקסטרינזיקה נפתרת
offline מאותן הקלטות בכל שלב.

## 12:30–14:00 — ההקלטות

```bash
./tools/run_live.sh --record captures/P1-20260818 --view visible
```

| סשן | מה | משך |
|---|---|---|
| P1 | אדם הולך, 2/5/8/12/15 מ', שני הצדדים | 20 דק' |
| P2 | עומד / יושב / כורע — המקרה שהורג clutterRemoval | 10 דק' |
| P3 | ‏2–3 אנשים, כולל חלוף זה על פני זה (המקרה שהפיל את walk4) | 15 דק' |
| C1 | סצנה ריקה + מאווררים, דלתות, וילונות | 20 דק' |

‏H1v (חושך/סנוור/מחסום — ולידציה ידנית בלבד) — בערב בבית; ‏live-0811 החשוך
הקיים מצטרף אליו.

## 16:00+ — ערב: הנתונים הטריים דרך הצנרת של אתמול

הכול כבר כתוב — זו הרצה, לא פיתוח:

1. ‏`run_channel_b.py P1-20260818 --lambda-v 0.5` — פעם ראשונה עם דופלר חי;
   **שער D2 הסופי** (שער 3 של PERCEPTION-PLAN): מיתות בגבול פיזי ≥80%,
   והפעם גם ההצטלבויות של P3 עם ציר v·λ.
2. ‏`run_teacher.py` + `build_dataset.py` על P1–P3 — דאטהסט אמיתי ראשון
   (ואם H2 הצליח — ‏COCO תרמי 16-bit נקי).
3. פתרון V2 של האקסטרינזיקה מתחנות H5 (‏`radar_calib_web.py`) → הרצת
   `run_associate.py` מחדש — **שער D3** ב-±20px.
4. עדכון `WORK-PLAN.md` + דף התוכנית לפי מה שעבר.

## ערב מאוחר / מחר — H6: ‏rig_health — הטמעת הקוד של יעל (שלבים 1–3 מאוחדים)

מקור: ‏ValiSight ענף `yael`, נעול על קומיט `a54b7df`. עבודת שולחן (~1.5–2 ימים);
לא נוגעת בשום שער של היום — אבל **הפלטים של היום הם קלטי ה-backtest שלה**:
‏`bias-20260818.json` מ-H1 ותחנות ה-TCR של H5 הם בדיוק מה שהמוניטור יאכל.

**עיקרון-על: המוניטור קורא, לא פותר.** אף Constraint לא כותב לכיול; הרוטציה
הקפואה נשארת קפואה. כל הספים ב-`calib-artifacts/health-budgets.json` — לא בקוד.

### א. השלד

- ‏vendor של הליבה (stdlib בלבד, ~340 שורות): ‏`check.py`, `stage.py`,
  `runner.py`, `calibration/constraints.py` → ‏`perception/health/` + קובץ
  PROVENANCE (ריפו/ענף/קומיט/תאריך).
- **לא** מעתיקים את `context.py` (geo-י) — כותבים `RigContext` רזה: נתיבי
  calib-artifacts, סשן, תקציבים.
- ‏`rig_health.py` בתבנית ה-main שלה: ‏fail-fast כברירת מחדל, ‏`--diagnose`
  מריץ הכול; ‏exit code הוא השער. שלבים לא-מחוברים = SKIPPED, לא PASS ולא FAIL.
- מייבאים גם את `test_imu_calibration.py` ודפוסי ה-conftest שלה.

### ב. שלושת גלאי-הסטייה

| Constraint | ניזון מ- | בדיקות |
|---|---|---|
| `RadarBoresightConstraint` | ‏`bias-20260818.json` + תחנות H5 | טווח TCR ‏1.50±0.02 מ'; ‏\|az\|≤0.5°; ‏du ±50px לתחנה; תחנה סוררת; דגנרציית פריסה (מ-`targets.py` שלה — שני צידי אזימוט, פיזור) |
| `ThermalRgbConstraint` | פריימי כיול נוכחיים מול R ‏3.65°, ‏\|t\|‏ 28.1 מ"מ | שארית רה-פרויקציה ≤0.7 th-px ‏(baseline ‏0.39–0.49); מספר התאמות; כיסוי; נקודה סוררת |
| `TemporalConstraint` | ‏td שמור + ‏`radar_listen --stage1` | ‏td מול השמור; ‏0 בייטים אבודים; קצבי פריימים בטווח |

### ג. גלאי הכבידה (מותנה בחומרה)

**קודם לאמת: ה-IMU של ה-N6 נגיש ומשודר בלינק** (וקטור אחד לסשן — רוחב פס
זניח). כן → ‏`imu.py` שלה כמעט as-is: שומרים כיוון-כבידה ייחוס בכיול, משווים
כל סשן. לא → מדלגים; לא חוסם כלום.

**שער H6 (backtest):** ‏`calib4` + ‏`live-0811` + הסשנים הטריים של היום עוברים
נקי; כיול-הזכוכית הישן **חייב להיכשל** על בדיקת הפריסה. אזעקות שווא על
ה-baseline → מכיילים תקציבים, לא קוד. בסיום: שלושת האודיטורים (purity,
extrinsic, temporal) מאשרים שהמוניטור read-only.

מרגע שעובר: ‏`rig_health.py` רץ בפתיחת ובסגירת כל סשן, דוח JSON נשמר ליד
ההקלטות — הקלטה תקפה = ממוסגרת בין שני דוחות ירוקים.

## כללי ההכרעה (ירושה מאתמול, בתוקף)

| נכשל | עושים |
|---|---|
| טבלת פאזה (08:15) | עוצרים. לא נוסעים, לא מקליטים |
| לפטון 16-bit (09:15) | מקליטים 8-bit; רק ערוץ A' נדחה |
| אין אתר (10:00) | מקליטים C1+P2 בבית; אתר מחר |
| שער היציאה (10:45) | אין P1; מאבחנים את Mode P |
| אקסטרינזיקה (11:30) | **ממשיכים להקליט** — נפתר offline |
| ‏H6 ‏backtest (ערב/מחר) | לא חוסם כלום היום; אזעקות שווא → מכיילים תקציבים, כשל אמיתי → ממצא כיול |
