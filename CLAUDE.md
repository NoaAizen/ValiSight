# CLAUDE.md

קובץ הקשר שנטען בכל סשן. קרא אותו לפני כל עבודה על הריפו.

## ארכיטקטורה

שלוש שכבות קשיחות: **app → adapters → core**.

- **core**: טהור. אפס ייבוא חומרה, אפס I/O, אפס רשת. 100% ניתן לבדיקה בלי חומרה.
- **adapters**: כל מגע עם UART, ROS, קבצים, רשת.
- **app**: הרכבה בלבד.

כיוון התלות חד-סטרי. ייבוא בכיוון ההפוך הוא באג, לא סגנון.

## עקרון-על

אין פיקסלים מסונתזים בצינור ההחלטה. מודלים גנרטיביים מותרים אך ורק לפלט
תצוגה לאדם, לעולם לא כקלט לתכנון. LUT דטרמיניסטי על ערכים רדיומטריים אינו
סינתזה.

## מלכודות פיזיקה מתועדות — כל אחת מהן עברה בעבר סקירת אדם ונכשלה

1. דעיכת עוצמה למטרה נקודתית מונוסטטית היא **1/R⁴**, לא 1/R².
2. רזולוציה זוויתית של ULA מתרחבת כ-**1/cos θ** לרוחב ה-FOV. היא אינה קבועה.
3. IWR1843, TDM MIMO, 3TX, 16 לולאות: מהירות חד-משמעית מקסימלית **±0.67 מ"ש**.
   הולך רגל ב-1.4 מ"ש מתקפל ל-0.06 מ"ש ומסווג כסטטי. זהו מצב כשל ידוע.
4. `STATIC_V=0.25` תקף רק כשהרדאר נייח. על פלטפורמה נעה ההנחה קורסת לחלוטין.
5. σ_el ≈ 12° ב-IWR1843. Elevation הוא צוואר הבקבוק לדיוק ברמת פיקסל.
6. HawkEye top-m skip דורש מסלול heatmap גולמי מ-ADC. ענן נקודות נותן
   0.05–0.17 היפותזות לקרן מול 1.28–1.45 מ-heatmap. צוואר הבקבוק הוא
   CFAR/peak-grouping בקושחה, לא מערך האנטנות.

## מלכודות מפה ו-prior

7. `pyproj` לא זורק חריגה כשרשת EGM2008 חסרה — הוא מחזיר ערכים ללא שינוי.
   בישראל זה מייצר היסט קבוע של 17–18 מ' שמחקה שגיאת כיול. כל מסלול
   שנוגע בגובה גיאואידי חייב לעבור דרך `_assert_geoid_live()`.
8. GLO-30 הוא DSM וכולל גבהי מבנים. ב-`osm_extrude` חובה Z = ground + height_osm
   כהשמה, לא כחיבור. ground מוערך מאחוזון טבעת ההיקף.
9. שגיאת סקאלה במפה מייצרת הטיה שקטה ב-β שאינה מפריעה ל-R². R² אינו
   מטריקת תקינות עבור מסלול זה.
10. GLO-30 ב-30 מ' מספיק למישור קרקע ולגובה עצמי בלבד. אינו מספיק
    לשאריות ברמת מכשול ב-epsilon אשכול של 1.2 מ'.

## מלכודות כיול

11. `td` (כיול זמני) לא נפתר במשותף עם אקסטרינזיקה גיאומטרית — תרגום
    מצומד-תנועה מקלקל את הפתרון.
12. Reprojection RMS הוא מטריקה מטעה בניסוח EIV. המטריקות הנכונות הן
    `cov_ext` מהמשלים של Schur ו-`cond(H_ext)`.
13. אתחול מטרה מרדאר בלבד לפי elevation מפיל את האופטימייזר למינימום
    מקומי — שגיאות גובה של כ-6 מ' בטווח 30 מ'.

## פילוסופיית בדיקות

מבחני יחידה pass/fail נפרדים מ-characterization sweeps שמייצרים עקומות
ותקציבי שגיאה. מבחן שקורא לפונקציה פנימית ועוקף את הצינור המלא הוא
מבחן שבור גם כשהוא ירוק.

## מה אסור

- אל תציע סוכן "מנהל פיתוח" או סוכן שמסכם עבודה שלא אימת.
- אל תבצע עבודה איטרטיבית בתת-סוכן מבודד.
- אל תשנה ערך ספי כדי לגרום למבחן לעבור.
