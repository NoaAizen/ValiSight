# הקלטת החזקות IMU — 18.8.2026 (ליעל)

הוקלט חי מה-N6 דרך הגשר (`bridge_rx`), 110 ש', IMU 200 Hz, 0 אובדן.

- `imu.csv` — כל הדגימות (פורמט לפי `FOR_YAEL.md`; `ts_src=0` → טיק = 1 µs).
- `holds.json` — הפלט של `imu_holds.py`: 8 החזקות סטטיות בצורת `RigOrientation`
  (`camera_gravity` = null, כמוסכם). 20/28 זוגות ≥15° — מספיק לפותר.
- `frames/<hold>/<seq>_thermal.bin`, `<seq>_rgb.bin` — הפריימים שצולמו בתוך חלון כל
  החזקה (thermal = 160×120 GRAY8 אחרי כותרת התמונה של הפרוטוקול; rgb = JPEG).
  `frames_index.json` — טווח ה-seq של ה-IMU ומספר הפריימים לכל החזקה.

## התנוחות (לפי כיוון הכובד ב-IMU)
| החזקה | a (mg) | מה זה |
|---|---|---|
| 01–03 | (+1002, 4, 15) | שטוח על השולחן — **+X של ה-IMU = למעלה** (המד-תאוצה מודד כוח-נגד: הציר שמצביע למעלה קורא +1g) |
| 04, 05 | (−160, +980, −20/−180) | על הצד — **+Y = למעלה** (05 נטוי עוד ~10°) |
| 06, 07 | (+75, −19, −988) | על הפאה — **−Z = למעלה** (+Z למטה) |
| 08 | (+1002, 3, 2) | שטוח שוב |

## מה לדעת
- ההחזקות 04–07 קצרות (2–6 ש' כל אחת, ~400–1100 דגימות) — יציבות (ג'ירו < 3°/ש',
  סטיית תקן 3–10 mg) אבל קצרות מהמומלץ (10–20 ש'). לסבב הבא: להניח ולספור עד 20.
- **לוח הכיול לא היה בפריים** — לכן `camera_gravity` לא ניתן לחישוב מההקלטה הזאת;
  היא טובה לבדיקת צירים ולפותר ה-IMU, לא לכיול מצלמה↔IMU סופי.
- לקח: המתקן חייב לנוח על משהו — ביד הג'ירו מראה 9–11°/ש' ואף החזקה לא נספרת.
