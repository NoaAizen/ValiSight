# הכיולים של ValiSight — רדאר + מצלמה

כל הכיולים במערכת: מה כל אחד מכייל, איפה הקוד, מה הערכים הנוכחיים, איך מריצים
מחדש, ואיזה מלכודות כבר נפלנו בהן. נכון ל-2026-08-09, מתוך ענף `hagai`.

## טבלת מצב — מה מכויל ומה לא

| כיול | קובץ קוד | ערכים שמורים | מצב |
|---|---|---|---|
| גיאומטרי רדאר↔מצלמה (K, dist, R, t) | `calibrate_radar_camera.py` | `calib_rgb.json` / `calib_thermal.json` | **לא בוצע** — הקבצים לא קיימים |
| שיוך אזימוטלי ללא כיול (fallback) | `core/fusion/azimuth.py` | קבועים בקוד | **פעיל** — זה מה שה-overlay החי רץ עליו |
| בסיס החזרים של הרדאר (reflectivity) | `radar_calibration.py` | `configs/refl_baseline.json` | **קיים ושמור** |
| תרמי: שורות מתות + destripe | `src/lepton_fix.py` | קבועים בקוד | **פעיל** |
| תרמי: ריסון AGC | `src/lepton_fix.py` | 200 (בקוד) | **פעיל** |
| תרמי: FPN (רעש תבנית קבועה) | `src/lepton_fix.py` | אין כיול תקף | **אין** — שתי דגימות נדחו |
| IMU (הטיה, רעש) | `src/attitude.py` | נמדד ותועד בקוד | **נמדד** |
| עיגון מפה + סקאלה | `map_anchor.py` | `meta.json` פר-סשן | פר-סשן |

---

## 1. כיול גיאומטרי רדאר↔מצלמה — `calibrate_radar_camera.py`

הכיול ה"אמיתי" שמאפשר להטיל נקודת רדאר על פיקסל במצלמה. שני שלבים,
שניהם נשמרים לקובץ `calib.json` אחד פר מצלמה:

1. **אינטרינזיקה** — צילומי לוח שחמט ⟵ מטריצת מצלמה K + עיוות (dist).
   - לוח: 9×6 פינות פנימיות, ריבוע 25 מ"מ (`DEFAULT_BOARD`, `DEFAULT_SQUARE_M`)
   - דרושות ≥5 תמונות עם הלוח מלא, מזוויות ומיקומים שונים
2. **אקסטרינזיקה** — ≥6 התאמות (מומלץ 15+) בין נקודת רדאר (x,y,z במטרים,
   בפריים של הרדאר) לפיקסל (u,v) שבו אותה מטרה נראית ⟵ solvePnP ⟵ R, t
   כך ש-`P_cam = R @ P_radar + t`.
   - קריטריון קבלה: שגיאת reprojection ממוצעת < ~2 פיקסלים
   - חובה לאמת בעומק **חדש** שלא שימש לכיול
   - לאסוף התאמות על פני כל ה-FOV וב-2–3 עומקים

**מוסכמות צירים** (מקור לבלבולים — לשנן):
- רדאר: x = קדימה, y = שמאלה, z = למעלה
- מצלמה (OpenCV): x = ימינה, y = למטה, z = קדימה
- R בולע גם את תמורת הצירים וגם את סטיית ההרכבה; t = זרוע המנוף בין החיישנים במטרים.

**שימוש מקוד** (חבילת fusion קוראת לאלה):
```python
K, dist, rms = calibrate_intrinsics(images)
R, t, err = calibrate_extrinsics(radar_pts, pixel_pts, K, dist)
save_calib("calib_rgb.json", K, dist, R, t, reproj_err=err)
calib = load_calib("calib_rgb.json")
uv, z_cam = project(points_radar, calib)
```

**מצב נוכחי: הכיול הזה מעולם לא בוצע על הריג** — `calib_rgb.json` ו-
`calib_thermal.json` לא קיימים בשום מקום. `fusion/camera.py` מחפש אותם בשורש
הריפו ומחזיר None; לכן ההיתוך החי רץ על המסלול חסר-הכיול (סעיף 2).

**מלכודות מתועדות (CLAUDE.md, כולן נכשלו בסקירת אדם בעבר):**
- כיול זמני `td` **לא** נפתר במשותף עם האקסטרינזיקה — תרגום מצומד-תנועה מקלקל את הפתרון.
- Reprojection RMS הוא מטריקה מטעה בניסוח EIV; המטריקות הנכונות: `cov_ext`
  מהמשלים של Schur ו-`cond(H_ext)`.
- אתחול מטרה מרדאר בלבד לפי elevation מפיל למינימום מקומי (~6 מ' שגיאת גובה
  בטווח 30 מ') — σ_el של ה-IWR1843 הוא ~12°, וזה צוואר הבקבוק לדיוק פיקסלי.

## 2. שיוך אזימוטלי ללא כיול — `core/fusion/azimuth.py` (מה שרץ בפועל)

ה-fallback שה-overlay החי עובד עליו כל עוד אין `calib.json`: אזימוט pinhole של
מרכז תיבה תרמית מול אזימוט צנטרואיד אשכול רדאר, שיוך חמדני 1:1, ואז אותו
שילוב inverse-variance מונחה-אי-ודאות של המסלול המכויל. **הרדאר נותן את הטווח**
(לתמונה תרמית של 160 פיקסלים אין טווח).

הקבועים (אלה ערכי הכיול בפועל):

| קבוע | ערך | מקור |
|---|---|---|
| `LEPTON_HFOV_DEG` | 57.0° | דאטהשיט FLIR Lepton 3.5 |
| `MATCH_AZ_DEG` | 10.0° | פער אזימוט מקסימלי לשיוך (משוקף מ-`fusion_core`) |
| `yaw_offset_deg` | 0.0 | כמה בורסייט הרדאר מוטה ימינה מציר המצלמה — לא נמדד עדיין |
| `hfov_deg` (RGB, `fusion_core.CameraGeometry`) | 60.0° | ברירת מחדל |

- אזימוט מצלמה של אשכול: `atan2(-cy, cx) + yaw_offset` (מעלות, + = ימינה מהמרכז).
- הנוסחאות משוכפלות בכוונה מ-`fusion_core.CameraGeometry` (אי אפשר לייבא — גורר
  cv2); **טסט-סחיפה** הוא שומר הזהות היחיד. שינוי במקום אחד בלי השני ישבור אותו.

## 3. בסיס החזרים אמפירי של הרדאר — `radar_calibration.py`

מכייל את ציון ה"חומר" (reflectivity) של נקודות רדאר. הבעיה שהוא פותר: המדד
הישן `snr + 40·log10(R)` הניח דעיכת 1/R⁴, אבל הדמו של TI מדווח רק *גילויי*
CFAR — נקודות חלשות נעלמות במקום להיראות חלשות, אז ה-SNR המדווח צמוד לרצפת
ה-CFAR בכל טווח (נמדד על 25 אלף נקודות: חציון 12–15dB מ-0.4 עד 12 מ',
קורלציה עם 40log10(R) = ‎−0.18). התוצאה: 96.8% מתוויות החומר הישנות היו
ניתנות לשחזור מהטווח בלבד — מד-מרחק, לא מד-חומר.

התיקון: ציון מול מה שהחיישן *בדרך כלל* מדווח באותו טווח, נמדד מהלוגים של
הריג הזה עצמו:

```
score_db = signal − baseline(range)
```

- `signal` = ‏snr + noise (רמה מוחלטת, TLV 7) כשיש; אחרת snr גולמי
- ‏0dB = "חזק כמו ההחזר הסטטי החציוני בטווח הזה"; מתכת הרבה מעל, בולעים מתחת
- בלתי-תלוי-טווח מבנייה — בלי מודל התפשטות

**פרמטרים:** רוחב סל `BIN_M = 0.5` מ'; סל עם פחות מ-`MIN_BIN_PTS = 40` דגימות
נזרק (אינטרפולציה); ‏fallback כשאין קובץ: ‏`FALLBACK_SNR_DB = 14.0`.

**קובץ שמור:** `configs/refl_baseline.json` — קיים ועוקב בגיט.

**רענון** (אחרי שנצברים עוד סשנים מוקלטים):
```
python radar_calibration.py            # סורק logs/ וכותב configs/refl_baseline.json
```

## 4. כיולי התרמית (FLIR Lepton 3.5) — `src/lepton_fix.py`

### 4א. שורות מתות ושורות מוסטות

שתי רשימות נפרדות — **אסור לבלבל ביניהן**:

- `DEAD_ROWS = (6, 12, 28, 35, 36, 55, 56, 57, 58, 59, 60, 61, 62, 63)` —
  ‏14 שורות בלי שום מידע סצנה. מתוקנות באינטרפולציה (`repair()`).
- `OFFSET_ROWS = (5, 11, 31, 38, 43, 54)` — ‏6 שורות שרק מפולסות לא נכון:
  יש בהן סצנה אמיתית. מקבלות תיקון רמה בלבד (`destripe()`) — **לא** אינטרפולציה,
  אחרת זורקים מידע אמיתי.
- כלל מהגלאי: שורה מתה = דבק לקישוריות בלבד, אפס ראָיה. רצועות הטמפרטורה מתייגות בלבד.
- `measure_pixel_offsets()` מודד את ההסטים מסדרת פריימים.

### 4ב. ריסון AGC (הבהוב)

- המקור להבהוב: ברירת המחדל של הקושחה `AGC_HEQ_DAMPENING_FACTOR = 0` — שיווי
  ההיסטוגרמה חושב מחדש כל פריים.
- התיקון: כתיבה לרגיסטר CCI ‏`0x0124` את הערך **`AGC_DAMPING = 200`**.
  מרסנים — לא מכבים את ה-AGC.
- ניתן לשינוי בזמן ריצה: `live_server.py --agc-damping N`.

### 4ג. כיול FPN (רעש תבנית קבועה) — עדיין אין כיול תקף

- דרישות: ≥`FPN_MIN_FRAMES = 40` פריימים, ושער שינוי-סצנה
  `FPN_MIN_SCENE_CHANGE = 0.12` בין תחילת האיסוף לסופו.
- **חור ידוע (2026-07-29):** השער עובר גם כשאדם זז מול מצלמה סטטית — הצללית
  הממוצעת שלו נצרבת במפת ה-FPN (std ‏12.4 DN לעומת ~2 DN של FPN אמיתי).
  השער מוכיח שהתצוגה השתנתה, לא שהמצלמה זזה.
- שתי דגימות פסולות שמורות כראיה: `src/cfg/lepton_fpn_REJECTED_person_burnin_20260729.npy`,
  `src/cfg/lepton_fpn_REJECTED_scene_edges_20260729.npy`.
- **הנוהל הנכון לאיסוף: פנורמה — המצלמה זזה, אף אחד בפריים.**

## 5. כיול IMU (על ה-N6) — `src/attitude.py`

ערכים שנמדדו על הלוח הזה במנוחה:

| גודל | ערך נמדד | משמעות |
|---|---|---|
| ‏\|a\| במנוחה | ‏1004 mg, ‏sd ‏3.6 mg | ~0.2° רעש זוויתי — roll/pitch מצוינים, בלי דריפט |
| הטיית ג'יירו ציר Z | ‏−0.43°/s | ‏26°/דקה דריפט yaw |

- אין מגנטומטר במודול ⟵ ‏yaw הוא **יחסי** בלבד, עם אי-ודאות גדלה; לאפס אותו לעיתים קרובות.
- מיקום מה-IMU בלתי אפשרי עקרונית: הטיית שארית של ‏2 mg נותנת ‏0.98 מ' שגיאה
  אחרי 10 שניות, ‏35 מ' אחרי דקה (‏0.5·b·t²).

## 6. עיגון מפה וסקאלה — `map_anchor.py`

כיולים פר-סשן שנכתבים ל-`meta.json` של הסשן (`"map": {image, x_px, y_px, heading_deg, lat, lon}`):

1. **עיגון GPS** (עם מפה מג'יאורפרנסת מ-`map_geotiles.py`):
   `python map_anchor.py --latlon 31.76465 35.19134 --heading 350`
   ‏(heading = אזימוט מצפן, 0 = צפון, עם כיוון השעון)
2. **עיגון בקליק** על כל תמונת מפה: קליק על מיקום הרדאר, קליק שני לכיוון, S לשמירה.
3. **כיול סקאלה חד-פעמי** למפה לא-מג'יאורפרנסת:
   `python map_anchor.py --calibrate 50` — שני קליקים במרחק אמיתי ידוע (כאן 50 מ').
   (`maps/campus_sat.png` שנוצר מ-`map_geotiles.py` לא צריך את זה לעולם.)

**מלכודות גיאו (CLAUDE.md):**
- ‏`pyproj` לא זורק חריגה כשרשת EGM2008 חסרה — מחזיר ערכים ללא שינוי. בישראל
  זה היסט שקט של 17–18 מ' שמחקה שגיאת כיול. כל מסלול גובה גיאואידי חייב לעבור
  `_assert_geoid_live()`.
- ‏GLO-30 הוא DSM (כולל גבהי מבנים): ‏Z = ground + height_osm כ**השמה**, לא חיבור.
- שגיאת סקאלה במפה ⟵ הטיה שקטה ב-β ש-R² לא מגלה. ‏R² אינו מטריקת תקינות למסלול הזה.

## 7. מה שקרוב לכיול אבל הוא לא — כדי שלא יתבלבל

- **קונפיגי צ'ירפ** (`configs/iwr1843_*.cfg`, ‏`src/cfg/odom_*.cfg`,
  ‏`src/stock_iwr1843.cfg`) — הגדרות עבודה של הרדאר, לא כיול. אבל חשוב:
  ‏`live.sh` שולח כברירת מחדל את `stock_iwr1843.cfg` (‏v_max ‏0.974 מ'/ש)
  כדי שכל הקלטה תדע איזה קונפיג ייצר אותה.
- **סנכרון זמן** (`core/timesync/`) — משמעת שעון, לא כיול: זמן דגימה משוחזר
  מאינדקס הפריים (`anchor + n·period`), לא מזמן ההגעה; שעון מונוטוני יחיד;
  ‏`SyncBuffer` מזווג תרמי (~8.7Hz) עם רדאר לפי חותמת זמן.
- **‏`--chest-range` ב-vitals** — לא כיול אלא עקיפת מלכודת: קיר ב-1.0–1.5 מ'
  מחזיר ~10dB יותר מהחזה וגונב את בוחר-ה-bin דרך multipath.

---

## סיכום — מה הצעד הבא בכיולים

1. **הפער הגדול: הכיול הגיאומטרי (סעיף 1) מעולם לא בוצע.** עד שהוא ייעשה,
   ההיתוך רץ על אזימוט בלבד עם `yaw_offset = 0` לא-מדוד. לביצוע: לוח שחמט
   לאינטרינזיקה, ‏15+ התאמות רדאר↔פיקסל ב-2–3 עומקים לאקסטרינזיקה, אימות
   בעומק חדש.
2. **אין כיול FPN תקף לתרמית** — לאסוף בפנורמה (המצלמה זזה, אף אחד בפריים).
3. **‏`refl_baseline.json` כדאי לרענן** אחרי שנצברים סשנים חדשים על הריג.
